[build]
  command = "npm install && npm run build:css && hugo --gc --minify --baseURL \"$URL\""
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.164.0"
  HUGO_EXTENDED = "true"
  NODE_VERSION = "22"

[context.production.environment]
  HUGO_ENV = "production"